import { header } from '@/locales/en/header';
import { login } from '@/locales/en/login';
import { main } from '@/locales/en/main';
import { navbar } from '@/locales/en/navbar.ts';

export const en = {
  login,
  header,
  main,
  navbar,
} as const;
